# Dependências (raiz e subpastas: br12c/, local/, ...)
node_modules/

# Cache de build/dev/test do Vite e Vitest (raiz e subpastas)
.vite/

# Config do agente especifica da maquina (o launch.json fica versionado de proposito)
.claude/settings.local.json

# Relatorio de cobertura: o vitest roda com raiz no projeto, entao `test:coverage`
# escreve aqui, nao em local/ (onde ja havia regra).
coverage/
