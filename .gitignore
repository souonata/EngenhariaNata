# Dependências (raiz e subpastas: br12c/, local/, ...)
node_modules/

# Cache de build/dev/test do Vite e Vitest (raiz e subpastas)
.vite/

# Config do agente especifica da maquina (o launch.json fica versionado de proposito)
.claude/settings.local.json

# Relatorio de cobertura: o vitest roda com raiz no projeto, entao `test:coverage`
# escreve aqui, nao em local/ (onde ja havia regra).
coverage/

# Normas (NBR/EN/HR Wallingford) usadas como referencia do calha: protegidas por direitos, nunca commitar
calha/*.pdf
calha2/

# Referências privadas do calha (normas protegidas por direitos, texto extraído delas) e
# experimentos locais: nunca commitar.
calha/referencias/*.pdf
calha/referencias/auditoria-2026-09-13/nbr10844-texto-extraido.txt
calha/tmp/
