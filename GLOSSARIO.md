# 📚 Glossário de Programação para Crianças

## 🎯 Como Usar Este Glossário

Quando você vir uma palavra em **inglês** no código, procure aqui para saber o que significa em **português**!

---

## 🏗️ **Classes CSS** (Nomes de Estilos)

### Estrutura da Página
- `.container` = **recipiente** (caixa que guarda todo o conteúdo)
- `.header` = **cabeçalho** (parte de cima da página)
- `.footer` = **rodapé** (parte de baixo da página)
- `.card` = **cartão** (caixinha que guarda informação)
- `.card-header` = **cabeçalho do cartão**
- `.card-content` = **conteúdo do cartão**

### Tela Inicial (index.html)
- `.device-screen` = **tela-do-dispositivo** (simula tela de celular)
- `.barra-status` = **barra-de-status** (barra do topo com horário)
- `.home-screen` = **tela-inicial** (onde ficam os apps)
- `.apps-grid` = **grade-de-apps** (organiza os ícones em grade)
- `.app-icon` = **ícone-do-app** (cada ícone clicável)
- `.app-name` = **nome-do-app** (texto embaixo do ícone)
- `.icon` = **ícone** (recipiente do desenho SVG)
- `.dock` = **dock** (barra inferior, igual iPhone)
- `.logo-container` = **recipiente-da-logo**
- `.logo-icon` = **ícone-da-logo**
- `.logo-text` = **texto-da-logo**
- `.logo-highlight` = **destaque-da-logo** (parte colorida)

### Horário
- `.horario` = **horário** (mostra hora e data)

### Calculadora (mutuo/index.html)
- `.language-selector` = **seletor-de-idioma**
- `.lang-btn` = **botão-de-idioma**
- `.exemplos-section` = **seção-de-exemplos**
- `.exemplos-header` = **cabeçalho-de-exemplos**
- `.btn-fechar-exemplos` = **botão-fechar-exemplos**
- `.btn-voltar-exemplo` = **botão-voltar-exemplo**
- `.controles-rapidos` = **controles-rápidos**
- `.controle-item` = **item-de-controle**
- `.arrow-btn` = **botão-de-seta** (setas ⬆️⬇️)
- `.arrow-up` = **seta-para-cima**
- `.arrow-down` = **seta-para-baixo**
- `.slider-container` = **recipiente-do-controle-deslizante**
- `.graficos-container` = **recipiente-de-gráficos**
- `.tabela-container` = **recipiente-de-tabela**
- `.system-card` = **cartão-de-sistema** (SAC, Price, Americano)
- `.special-features` = **funcionalidades-especiais**
- `.special-feature` = **funcionalidade-especial**

### Sobre (sobre/index.html)
- `.tech-list` = **lista-de-tecnologias**
- `.features-grid` = **grade-de-funcionalidades**
- `.feature` = **funcionalidade**
- `.feature-icon` = **ícone-da-funcionalidade**
- `.file-tree` = **árvore-de-arquivos**
- `.file-item` = **item-de-arquivo**
- `.file-desc` = **descrição-do-arquivo**
- `.design-principles` = **princípios-de-design**
- `.principle` = **princípio**
- `.principle-number` = **número-do-princípio**
- `.test-scenarios` = **cenários-de-teste**
- `.test` = **teste**
- `.test-icon` = **ícone-de-teste**
- `.learning-topics` = **tópicos-de-aprendizado**
- `.topic` = **tópico**
- `.formula-section` = **seção-de-fórmula**
- `.formula-desc` = **descrição-da-fórmula**
- `.formula-behavior` = **comportamento-da-fórmula**
- `.dev-section` = **seção-de-desenvolvimento**
- `.add-app-step` = **passo-para-adicionar-app**
- `.home-button-fixed` = **botão-início-fixo** (botão de voltar)

---

## ⚙️ **Funções JavaScript** (Coisas que o Código Faz)

### index-script.js
- `updateTime()` = **atualizarHorario()** (atualiza o relógio)
- `timeElement` = **elementoHorario** (elemento HTML do horário)
- `now` = **agora** (data/hora atual)
- `hours` = **horas**
- `minutes` = **minutos**
- `icon` = **icone** (cada ícone de app)

### mutuo-script.js
- `calcularEmprestimo()` = **calcular o empréstimo**
- `calcularPrice()` = **calcular sistema Price**
- `calcularSAC()` = **calcular sistema SAC**
- `calcularAlemao()` = **calcular sistema Alemão/Americano**
- `trocarIdioma()` = **trocar idioma** (PT ↔ IT)
- `ajustarValor()` = **ajustar valor** (com botões de seta)
- `formatarMoeda()` = **formatar moeda** (R$ ou €)
- `atualizarGraficos()` = **atualizar gráficos**
- `toggleExemplos()` = **alternar exemplos** (mostrar/esconder)

---

## 📦 **Variáveis Importantes** (Guardam Informação)

### Variáveis Comuns
- `valor` = **valor** (número)
- `taxa` = **taxa** (porcentagem)
- `prazo` = **prazo** (tempo)
- `idiomaAtual` = **idioma atual** ('pt-BR' ou 'it-IT')
- `moedaAtual` = **moeda atual** ('BRL' ou 'EUR')
- `valorEmprestimo` = **valor do empréstimo**
- `taxaMensal` = **taxa mensal**
- `numeroParcelas` = **número de parcelas**
- `tabelaAmortizacaoAtual` = **tabela de amortização atual**
- `dadosEmprestimo` = **dados do empréstimo**
- `traducoes` = **traduções** (dicionário PT/IT)

### Nomes de Elementos
- `button` = **botão**
- `input` = **entrada** (campo onde você digita)
- `label` = **rótulo** (texto que explica o campo)
- `select` = **seletor** (menu suspenso)
- `option` = **opção**
- `slider` = **controle deslizante**
- `chart` = **gráfico**
- `table` = **tabela**

---

## 🎨 **Propriedades CSS** (Estilos Visuais)

### Layout
- `display` = **exibição** (como mostrar: flex, grid, block)
- `flex` = **flexível** (organiza elementos de forma flexível)
- `grid` = **grade** (organiza em linhas e colunas)
- `position` = **posição** (onde colocar: relative, absolute, fixed)
- `width` = **largura**
- `height` = **altura**
- `padding` = **espaçamento interno**
- `margin` = **espaçamento externo**
- `gap` = **espaço** (entre itens de grid/flex)

### Aparência
- `background` = **fundo**
- `color` = **cor** (do texto)
- `border` = **borda**
- `border-radius` = **arredondamento da borda**
- `box-shadow` = **sombra da caixa**
- `opacity` = **opacidade** (transparência)
- `transform` = **transformação** (mover, girar, aumentar)
- `transition` = **transição** (animação suave)

### Texto
- `font-family` = **família da fonte**
- `font-size` = **tamanho da fonte**
- `font-weight` = **peso da fonte** (negrito)
- `text-align` = **alinhamento do texto**
- `line-height` = **altura da linha**

---

## 🔧 **Palavras Reservadas** (Palavras Especiais das Linguagens)

⚠️ **ESTAS NÃO PODEM SER TRADUZIDAS!** São palavras que JavaScript, HTML e CSS entendem:

### JavaScript
- `function` = palavra que começa uma função
- `const` = constante (valor que não muda)
- `let` = variável (valor que pode mudar)
- `var` = variável antiga
- `if` = se (condição)
- `else` = senão
- `for` = para (repetição)
- `while` = enquanto
- `return` = retornar
- `true` = verdadeiro
- `false` = falso
- `new` = novo
- `this` = isto
- `document` = documento HTML
- `window` = janela do navegador
- `console` = console (para debugar)
- `addEventListener` = adicionar ouvinte de evento
- `querySelector` = buscar elemento
- `getElementById` = pegar elemento por ID

### HTML
- `<div>` = divisão (caixa genérica)
- `<span>` = espaço (texto inline)
- `<button>` = botão
- `<input>` = entrada
- `<a>` = âncora (link)
- `<img>` = imagem
- `<svg>` = gráfico vetorial
- `<header>` = cabeçalho
- `<footer>` = rodapé
- `<section>` = seção
- `<article>` = artigo
- `<nav>` = navegação
- `<main>` = conteúdo principal
- `<aside>` = conteúdo lateral

### CSS
- `:hover` = ao passar o mouse
- `:active` = ao clicar
- `:focus` = ao focar (campo selecionado)
- `@media` = regra de responsividade
- `@keyframes` = animação
- `linear-gradient()` = gradiente linear
- `rgba()` = cor com transparência

---

## 🎯 **Conceitos Importantes**

### Mobile-First
Estratégia de design que começa pelo celular e depois adapta para telas maiores.

### Responsividade
Capacidade de um site se adaptar a diferentes tamanhos de tela (celular, tablet, desktop).

### Cache Busting
Técnica para forçar o navegador a baixar arquivos atualizados usando `?v=1.0.0`.

### i18n (Internacionalização)
Sistema para traduzir aplicações para múltiplos idiomas.

### Sistema de Amortização
Método de pagamento de empréstimos:
- **SAC**: Amortização constante, parcelas decrescentes
- **Price**: Parcelas fixas (mais comum)
- **Americano**: Só juros + principal no final

---

## 📖 **Referências Adicionais**

Para aprender mais, consulte:
- `copilot-instructions.md` - Documentação completa do projeto
- `sobre/` - Visão geral e guias de desenvolvimento
- `<section>` = seção
- `<article>` = artigo

### CSS
- Todas as propriedades CSS são em inglês e não podem ser traduzidas

---

## 💡 **Dica de Ouro**

Quando você ver uma palavra em inglês no código:

1. **É uma palavra reservada?** (JavaScript: function, const, if | HTML: div, span)
   - ✋ **NÃO MUDE!** A linguagem não vai entender

2. **É um nome escolhido pelo programador?** (classes: .app-icon, funções: updateTime)
   - 👍 **PODE TRADUZIR!** Isso ajuda a entender o código

3. **Na dúvida?** Procure neste glossário! 📚

---

## 🎮 **Exercício Prático**

Tente ler este código e traduzir só o que pode:

```javascript
function calcularSoma(primeiroNumero, segundoNumero) {
    const resultado = primeiroNumero + segundoNumero;
    return resultado;
}
```

**Pode traduzir:**
- `calcularSoma` = nome da função (escolhido pelo programador)
- `primeiroNumero` = nome da variável
- `segundoNumero` = nome da variável  
- `resultado` = nome da variável

**NÃO pode traduzir:**
- `function` = palavra reservada do JavaScript
- `const` = palavra reservada do JavaScript
- `return` = palavra reservada do JavaScript

---

✨ **Lembre-se**: Programar é como aprender uma nova língua. Algumas palavras você traduz, outras você aprende o significado em inglês!
