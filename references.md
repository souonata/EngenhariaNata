# Project References

A quick lookup of the main files and directories in this repository. Use the paths below when you need to inspect, edit, or run a script.

| Path |
|------|
| `README.md` – Project description and setup instructions |
| `aquecimento/` – Heating‑related pages and scripts |
| `arcondicionado/` – Air‑conditioning pages |
| `assets/css/` – Shared stylesheets |
| `assets/js/` – Helper scripts and vendor libraries |
| `bitola/` – Scale / metering UI |
| `bugs/` – Bug‑tracking documentation and UI |
| `config/versions.json` – Configuration version file |
| `fazenda/` – Farm / agriculture module |
| `helice/` – Helix (rotor) module |
| `index.html` – Main front‑end entry point |
| `index-script.js` – Core front‑end JavaScript |
| `index-styles.css` – Core styles |
| `local/` – Local development utilities and server setup |
| `mutuo/` – Mutual aid module |
| `robots.txt` – SEO directives |
| `sitemap.xml` – Site map |
| `sobre/` – About page |
| `solar/` – Solar / energy module |
| `src/` – Source code (e.g., server, utilities) |
| `sw.js` – Service worker |

---

> If you need the full content of a particular file, you can read it directly from the file system using the `Read` tool.

